How did viewing a diff between two versions of a file help you see the bug that
was introduced?

    Fill in your answer here

How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?

    Fill in your answer here

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like Google
docs does?
+ meaningful, understandable history
- danger of changing too much, thus the diff could be hard to understand

Why do you think some version control systems, like Git, allow saving multiple
files in one commit, while others, like Google Docs, treat each file separately?

    Fill in your answer here

How can you use the commands git log and git diff to view the history of files?
- use git log to get an overview of the project history, recent commits with their commit-IDs
- use git diff to see the changes between two different commits specified by their IDs

How might using version control make you more confident to make changes that
could break something?
- I can easily step back to an earlier version, to undo a change
- I can examine which change is introducing a bug
- I can explore new concepts for existing code, I can re-think existing code without the fear of breaking anything
  instead stepping forward by tiny little baby steps

Now that you have your workspace set up, what do you want to try using Git for?
- Delphi dependency injection samples
- Delphi MQTT client
- Delphi: MEAC filesystem data to DB
- report flagged STA values in classes, model set of system flags for that purpose
